import { AutoRegulation } from "../IAutoRegulation";

export class PercentRpeBased implements AutoRegulation {
  function nextWeight(): Number {
    //  calculate 1RM
    // next set rpe and reps
    // use projected 1rm to calculate rpe and reps
    return 1;
  }
}
